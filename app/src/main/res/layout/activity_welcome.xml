<?xml version="1.0" encoding="utf-8"?>
<!--
    Онбординг-экран.
    - Полноэкранный фон bg_welcome.png (centerCrop)
    - Нижний блок на полупрозрачной подложке (scrim) для читабельности
    - Кастомный чекбокс через @drawable/checkbox_onboarding
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0C10">

    <!-- Фоновое изображение на весь экран -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_welcome"
        android:contentDescription="@string/onboarding_bg_desc" />

    <!-- Заголовок сверху -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="vertical"
        android:paddingTop="56dp"
        android:paddingHorizontal="28dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="#F97316"
            android:textSize="26sp"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:letterSpacing="0.05" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/onboarding_subtitle"
            android:textColor="#94A3B8"
            android:textSize="13sp"
            android:fontFamily="monospace" />
    </LinearLayout>

    <!-- Нижний блок: статус разрешений + кнопка + чекбокс -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/onboarding_scrim"
        android:orientation="vertical"
        android:paddingTop="40dp"
        android:paddingBottom="36dp"
        android:paddingHorizontal="28dp">

        <!-- Статус разрешений -->
        <TextView
            android:id="@+id/tvPermissionStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/onboarding_need_permissions"
            android:textColor="#E2E8F0"
            android:textSize="13sp"
            android:fontFamily="monospace"
            android:lineSpacingExtra="4dp" />

        <!-- Кнопка дать разрешения -->
        <Button
            android:id="@+id/btnGrantPermissions"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="20dp"
            android:background="@drawable/btn_grant_bg"
            android:text="@string/onboarding_grant_button"
            android:textColor="#0A0C10"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:textSize="15sp"
            android:fontFamily="monospace" />

        <!-- Кастомный чекбокс + контрастный белый текст -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <CheckBox
                android:id="@+id/checkboxAgree"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:button="@drawable/checkbox_onboarding"
                android:enabled="false"
                android:minWidth="0dp"
                android:minHeight="0dp"
                android:paddingStart="0dp"
                android:paddingEnd="0dp" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="14dp"
                android:text="@string/onboarding_agree_text"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:fontFamily="monospace"
                android:lineSpacingExtra="2dp" />
        </LinearLayout>
    </LinearLayout>

</FrameLayout>
