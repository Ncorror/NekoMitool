NekoFlash 2.5.0 build attempt notes

Static check:
  python3 scripts/check_project.py
  PASS

APK build attempt in this container:
  ./gradlew --version
  FAILED because the container cannot resolve services.gradle.org and does not include a preinstalled Android SDK/Gradle cache.

This is an environment limitation of the preparation container, not a static project check failure. Build on a workstation with Java 17+, Android SDK and network access, or use GitHub Actions.
